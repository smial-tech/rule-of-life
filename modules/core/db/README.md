# DB folder

This folder contains the database module for the project. It includes the main logic for the database module, including connecting to the database, defining the database schema, and performing CRUD operations on the database. The db is based on the `drizzle-orm` package. Check drizzle-orm [documentation](https://orm.drizzle.team/docs/overview) for more information.